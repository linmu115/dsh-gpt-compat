---
{"id":"VER-cpa-start","kind":"verification","title":"本机 CPA 启动按钮验证","status":"current","summary":"验证离线状态、后台启动、重复点击、就绪检查和副本安装。"}
---

# 本机 CPA 启动按钮验证

范围：2026-09-18，账号模块 0.1.1；GPT 主插件以 0.5.0-dev.8 为基线，仅客户端增量，主插件 manifest 和会话格式入口字节不变。目标为 0.1.5-rc.2 副本 / web。源码入口为 `src/accounts/local.ts`、`src/accounts/host.ts`、`src/client/Accounts.tsx`。

- 类型检查通过；全套 85 项测试通过，1 项真实模型网络测试按配置跳过。覆盖并发启动合并、已运行不重复启动、未配置、非 CPA 端点、启动失败与超时、响应大小限制、离线按钮和账号刷新。
- 发行包已生成；安装包可独立加载并返回真实本机未运行状态。客户端附加可执行路径被拒绝。凭据与会话正文未写入报告。
- 已备份 profile 配置、原插件、客户端及正常关闭后的维护数据库。用户正常停止旧运行后，数据库为 closed、Launcher 回执 finalized/closed、旧实例不再发布身份；安装后的 19 个原有格式绑定校验文件保持不变。
- 首次在线尝试未使新模块生效。没有直接关闭运行时或强杀进程。用户正常停止后已完成安装，后续使用正式 Start 入口；是否就绪以本次启动身份核验为准。

真实页面、CPA 启动与重复请求验收待补充，不能用源码测试替代。

关联：[[MOD-accounts]]、[[HIST-cpa-start]]。
