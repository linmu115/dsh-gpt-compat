---
{"id":"VER-accounts","kind":"verification","title":"CPA 账号控制验证","status":"current","summary":"60 项插件测试通过；CPA 固定执行与管理接口测试通过；副本已连接 2 个真实账号，额度查询与页面检查通过。","relations":[{"relation":"verifies","to":{"record_id":"MOD-accounts"},"reason":"核对固定选择、凭据边界、额度及确认交互"}]}
---

# CPA 账号控制验证

2026-09-17，主插件 0.5.0-dev.3 + 可选账号模块 0.1.0。

- 插件全量 60 项测试通过，1 项可选 live 测试跳过；类型检查、客户端与服务端构建通过。
- 测试覆盖账号列表脱敏、管理密钥保存、固定选择及并发修订、额度未知、登录句柄与原生请求身份隔离；界面覆盖具名确认、取消不写入、切换携带读取时的修订。
- CPA auth 与请求处理包测试通过；固定管理接口测试通过。普通、流式、计数三种执行入口在所选账号失败时均不使用待用账号；暂停、缺失、旧请求身份不匹配均拒绝。
- CPA 完整 management 测试在 Windows 有既有环境变量大小写冲突：`TestSetLatestReleaseRequestHeaders/sets_GitHub_authorization` 同时设置 `GITHUB_TOKEN` 和 `github_token`，后者清空前者。本次未修改该测试，不宣称该包全量通过。
- 固定账号扩展已构建并安装在本机 CPA；管理合同只读请求返回 200。新增密码文件入口与 10 秒桌面心跳退出机制分离，原配置的管理密码保持不变。
- 副本通过正式 DSH 插件安装入口安装账号 bundle；19 项 Maintenance 受保护文件指纹一致。主实例未更改。

新增插件改变了实例清单指纹；已通过 Maintenance 正式 repair 操作重新验证接入，并由启动器正常启动。账号面板已自动连接本机管理凭据，列出 2 个真实账号；两者均返回 3 个有效额度窗口，51 个原会话可见。

真实 DSH 页面检查通过：无页面脚本错误，显示 6 条额度条；具名确认可取消，未发送切换/移除/登录写入。浅色和深色主题已检查，320px 独立插件容器无横向溢出（不代表整个宿主设置弹窗支持移动端）。截图仅在隔离浏览器中匿名化账号显示，未修改服务数据。

仍未为测试修改实际选号、移除账号或执行新的 OAuth 登录，不能把模拟测试当成这些真实操作的证据。CPA 保留原路由，待用户明确选择目标账号后进入固定模式。
