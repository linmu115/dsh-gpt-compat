---
{
  "id": "IF-profile",
  "kind": "interface",
  "title": "激活与请求配置合同",
  "status": "proposed",
  "summary": "向工具、提示词与提供商交付同一个步骤快照；安全边界后撤销或切换。",
  "progress": "in_progress",
  "relations": [
    {
      "relation": "references",
      "to": {
        "record_id": "REQ-gpt-compatibility"
      },
      "reason": "约束来自已确认的会话切换要求"
    }
  ]
}
---

# 激活与请求配置合同

这个接口让一次模型请求从提示词到工具结果始终使用同一套兼容配置。比如用户在请求执行中换模型，正在执行的工具仍按旧快照完成，新选择在下一安全步骤生效。

提供方为 [[MOD-routing|绑定与启停模块]]；计划消费者为 [[MOD-tools|工具模块]] 和 [[MOD-provider|提供商适配]]。这些是拟定的内部消费者，不代表已经有运行代码。

| 方向 | 语义内容 |
|---|---|
| 输入 | 会话身份、当前提供商与模型、绑定设置、宿主可用能力、步骤与取消信号 |
| 输出 | 是否启用、匹配策略、固定的协议与工具呈现、作用域身份和撤销责任 |
| 状态变化 | 匹配后准备与激活；选择变化待切换；当前调用收尾后撤销 |
| 可观察结果 | 当前生效配置、失败原因、所用版本与撤销结果可定位到会话步骤 |

匹配失败使用普通 DSH 配置。匹配成功但必要能力不支持时应明确暴露缺口，不能只显示已启用却漏掉必要工具。工具、提示词与最终请求路由不一致时不得发出混合请求。

接口变更影响工具可见性、已有进程句柄与历史结果解释，因此要核对三个消费者边界。确切函数名、事件扩展和设置 schema 尚未确定；已有宿主事件不能被旁路改写。

工具作用域与选择冻结部分已有实现；完整原生请求/返回协议尚未接入。具体边界与证据见 [[IMP-current|当前实现]]、[[VER-tools|工具首版验证]]。
