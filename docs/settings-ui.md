# GPT 兼容设置页与 DSH 样式统一

## 当前要求：自动提供模型

2026-09-17 用户澄清：设置区不需要模型下拉选择，也不需要手填提供商或模型。自动提供 GPT 的全部可用会话模型，真正的模型选择仍在会话页面。这替代此前绑定表单的交互要求。设置区只读展示自动检测列表并允许刷新；模型容量和推理档位读取 CPA 元数据，图片生成接口不混入会话模型。自动兼容限本插件注册的 Responses 提供商，不扩展到其它插件的同名 GPT 模型。

以下为此前样式及独立栏目修订记录；当前变更的验收结果另在文末说明。

用户要求：插件设置页的 UI 展示风格与 DSH 统一（2026-09-17，原页面截图）。

2026-09-17 后续要求：从当前副本移除旧的嵌入式 Codex Runtime 插件及其“Codex”设置栏目，把 GPT 适配设置独立放到设置窗口左侧。使用原生 `settings.section` 注册“GPT 适配”，页面包含模型绑定和 CPA 账号管理；模型页不再重复显示这些设置。旧 Runtime 的移除仅针对当前副本，保留会话与登录数据。

按钮、输入框和添加图标复用 DSH 平台提供的 `@deepseek-ai/dsh-client-ui-primitives`；布局对照 DSH `ModelsSection.module.css`，使用相同的主题变量、字号、边框与圆角。模型列表允许换行，完整展示较长的绑定内容。没有修改绑定匹配规则。

CSS 内嵌在经典脚本模块中，通过 Cordis effect 注册并在卸载时撤回，仅作用于 `.gpt-compat-settings`。共享组件来自宿主平台模块表，不引入第二份 React 或通过功能插件跨界导入组件。

## 验证

- TypeScript 类型检查和客户端经典脚本构建通过。
- 现有两项表单测试通过，覆盖按原修订保存，以及保存冲突后保留草稿。
- 在运行中的 rc.2 副本页面验证实际模块加载、16px 标题和卡片圆角、唯一的样式注册，以及编辑后放弃修改恢复原值。
- 使用已加载的 DSH 主题样式检查浅色、深色外观；深色验证仅修改独立浏览器的主题呈现属性，不改用户持久设置。
- 单独将插件容器设为 320px 检查无横向溢出；这不代表 DSH 整个设置弹窗已支持手机布局。

## 首次样式修订的副本交付范围

作为 `0.5.0-dev.3` 的前端修订，仅更新 rc.2 副本安装目录的 `lib/client.js`，经 DSH HMR 生效。安装清单、宿主入口和会话格式未修改，Maintenance 19 个已绑定文件的哈希全部保持一致，无需重新生成格式回执。私有本机验证材料位于 `artifacts/settings-ui-20260917`，不包含在公开仓库中。

## 独立栏目修订

客户端注册改为 `settings.section`（id 为 `gpt-compat`、顺序 15），由 DSH 设置容器提供导航、焦点与滚动；浏览器依赖改为设置容器，不再依赖模型页。当前副本通过正式插件安装流程移除旧 Runtime 并安装新包；插件清单变化需要重新采集/验证格式绑定材料和执行 Maintenance repair。原 Runtime 的会话数据保留，不将卸载插件解释为删除会话。

旧 Runtime bundle 曾同时注册 `dsh-runtime-support`。团队协作和会话分发仍依赖该服务，因此在副本中将它独立注册并保留；移除的是 `dsh-codex-runtime` 的嵌入式提供商和设置栏目，不能连带删除共享基础服务。

本次验收：类型检查、经典脚本构建和 4 项已有表单/账号交互测试通过。真实副本中“GPT 适配”独立可见、“Codex”栏目与原模型页挂件消失；绑定草稿可放弃，2 个 CPA 账号可见，无页面脚本错误。浅色、深色主题均检查；50 个非空会话 ID 全部保留，1 个空白入口重建，总数 51。重建后的 19 项格式绑定文件校验通过，主实例配置指纹不变。

## 自动模型目录验收

副本已安装自动发现版本：页面只读显示 gpt-5.5、gpt-6-astra、gpt-5.6-sol、gpt-5.6-terra、gpt-5.6-luna；没有提供商/模型输入框或下拉选择，保留刷新按钮和 CPA 账号模块。模型目录 API 返回各型号的推理档位；图片生成模型未混入。64 项离线测试通过、1 项可选真实生成测试跳过，类型检查与构建通过。前端服务依赖在真实页面验证时补齐了 `remote` 根服务，修复后的页面无脚本错误。

已检查浅色、深色以及 320px 模型卡片无横向溢出；两个 CPA 账号可见，50 个已有内容的会话 ID 全部保留。重新生成并验证 Maintenance 19 项绑定文件后，副本经正式启动器成功启动；主实例配置指纹不变。验证材料保存在本机 `artifacts/auto-models-final-20260917`，不进入公开仓库。此处验证模型发现与选择器能力，不声称五个模型均完成真实生成测试。

实际会话页已展开模型菜单，确认上述五个型号均可见；当前 GPT 的推理等级菜单可见 low、medium、high、xhigh、max。点击菜单外关闭，验证未改动当前会话选择。
